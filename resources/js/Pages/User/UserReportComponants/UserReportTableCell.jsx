import { Table } from "@devexpress/dx-react-grid-material-ui";

export default function UserReportTableCell(props) {
    // Default rendering for other columns
    return <Table.Cell {...props} />;
}
